post-checkout:
  commands:
    update-submodules:
      run: git submodule update --init --recursive

post-merge:
  commands:
    update-submodules:
      run: git submodule update --init --recursive
